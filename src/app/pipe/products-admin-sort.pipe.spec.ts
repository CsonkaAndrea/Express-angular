import { ProductsAdminSortPipe } from './products-admin-sort.pipe';

describe('ProductsAdminSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsAdminSortPipe();
    expect(pipe).toBeTruthy();
  });
});
