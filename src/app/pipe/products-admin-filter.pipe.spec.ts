import { ProductsAdminFilterPipe } from './products-admin-filter.pipe';

describe('ProductsAdminFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsAdminFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
